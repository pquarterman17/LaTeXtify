% Generated preamble -- regenerated every run. Do not edit; edit main.tex instead.
% elsarticle loads natbib itself; the citation mode is selected via CLASS
% options (numbers / authoryear), so natbib_options are folded in here.
\documentclass[\VAR{(class_options + natbib_options)|join(',')}]{\VAR{document_class}}
%% for pkg in packages
%% if pkg.options
\usepackage[\VAR{pkg.options|join(',')}]{\VAR{pkg.name}}
%% else
\usepackage{\VAR{pkg.name}}
%% endif
%% endfor
\bibliographystyle{\VAR{bibstyle}}
